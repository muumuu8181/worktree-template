# GAS テンプレートカタログ
## すぐに使える業務自動化テンプレート100選

---

## 📊 データ管理系（20個）

### 1. 在庫管理システム
```javascript
// 価格: 5万円 / 開発時間: 30分
function inventoryManagement() {
  const ss = SpreadsheetApp.getActive();
  const sheet = ss.getSheetByName('在庫') || ss.insertSheet('在庫');

  // 自動発注判定
  const data = sheet.getDataRange().getValues();
  for (let i = 1; i < data.length; i++) {
    if (data[i][2] < data[i][3]) { // 在庫 < 最小在庫
      sendOrderEmail(data[i][0], data[i][3] - data[i][2]);
    }
  }
}
```

### 2. 顧客管理（CRM）
```javascript
// 価格: 8万円 / 開発時間: 45分
function customerManagement() {
  // 顧客データの自動分類
  // RFM分析（最近購入日・頻度・金額）
  // 自動セグメント分け
  // メール配信リスト生成
}
```

### 3. 売上集計ダッシュボード
```javascript
// 価格: 6万円 / 開発時間: 40分
function salesDashboard() {
  // 日別・週別・月別集計
  // グラフ自動生成
  // 前年同期比較
  // 予実管理
}
```

### 4. 見積書自動生成
```javascript
// 価格: 5万円 / 開発時間: 30分
function createQuote() {
  // テンプレートから自動生成
  // PDF化
  // メール送信
  // 履歴管理
}
```

### 5. 請求書管理
```javascript
// 価格: 7万円 / 開発時間: 45分
function invoiceManagement() {
  // 請求書自動発行
  // 入金チェック
  // 督促メール自動送信
  // 売掛金管理
}
```

---

## 👥 人事・労務系（20個）

### 6. 勤怠管理システム
```javascript
// 価格: 8万円 / 開発時間: 50分
function attendanceManagement() {
  const sheet = SpreadsheetApp.getActiveSheet();

  // 出退勤記録
  function checkIn() {
    const now = new Date();
    const email = Session.getActiveUser().getEmail();
    sheet.appendRow([email, '出勤', now]);
  }

  // 月次集計
  function monthlyReport() {
    // 労働時間計算
    // 残業時間算出
    // 有給残数管理
  }
}
```

### 7. シフト管理
```javascript
// 価格: 7万円 / 開発時間: 45分
function shiftManagement() {
  // シフト希望収集
  // 自動調整
  // 人員過不足アラート
  // シフト表配信
}
```

### 8. 経費精算
```javascript
// 価格: 6万円 / 開発時間: 40分
function expenseManagement() {
  // 申請フォーム
  // 承認ワークフロー
  // 自動仕訳
  // 月次精算
}
```

### 9. 人事評価管理
```javascript
// 価格: 9万円 / 開発時間: 60分
function performanceReview() {
  // 評価シート配布
  // 集計・分析
  // フィードバック管理
  // 昇給・昇格判定
}
```

### 10. 採用管理
```javascript
// 価格: 7万円 / 開発時間: 45分
function recruitmentManagement() {
  // 応募者管理
  // 選考ステータス管理
  // 面接スケジュール調整
  // 自動返信メール
}
```

---

## 📈 営業・マーケティング系（20個）

### 11. リード管理
```javascript
// 価格: 8万円 / 開発時間: 50分
function leadManagement() {
  // リードスコアリング
  // 自動フォローアップ
  // ナーチャリングメール
  // コンバージョン追跡
}
```

### 12. 営業日報
```javascript
// 価格: 4万円 / 開発時間: 25分
function salesReport() {
  const template = {
    訪問件数: 0,
    商談件数: 0,
    受注件数: 0,
    受注金額: 0
  };

  // 日報自動集計
  // 週報・月報生成
  // KPI追跡
}
```

### 13. メールマーケティング
```javascript
// 価格: 7万円 / 開発時間: 45分
function emailMarketing() {
  // セグメント別配信
  // A/Bテスト
  // 開封率追跡
  // クリック率分析
}
```

### 14. アンケート集計
```javascript
// 価格: 5万円 / 開発時間: 30分
function surveyAnalysis() {
  // 自動集計
  // グラフ化
  // テキストマイニング
  // レポート生成
}
```

### 15. 競合分析
```javascript
// 価格: 6万円 / 開発時間: 40分
function competitorAnalysis() {
  // 価格調査
  // 機能比較表
  // 市場シェア分析
  // SWOT分析
}
```

---

## 🏭 製造・物流系（15個）

### 16. 生産管理
```javascript
// 価格: 9万円 / 開発時間: 60分
function productionManagement() {
  // 生産計画
  // 進捗管理
  // 品質記録
  // 歩留まり分析
}
```

### 17. 在庫最適化
```javascript
// 価格: 8万円 / 開発時間: 50分
function inventoryOptimization() {
  // ABC分析
  // 安全在庫計算
  // リードタイム管理
  // 発注点管理
}
```

### 18. 配送管理
```javascript
// 価格: 7万円 / 開発時間: 45分
function deliveryManagement() {
  // 配送スケジュール
  // ルート最適化
  // 配送状況追跡
  // 配送完了報告
}
```

### 19. 品質管理
```javascript
// 価格: 8万円 / 開発時間: 50分
function qualityControl() {
  // 検査記録
  // 不良率分析
  // 是正処置管理
  // トレーサビリティ
}
```

### 20. 設備保全
```javascript
// 価格: 6万円 / 開発時間: 40分
function maintenanceManagement() {
  // 点検スケジュール
  // 故障履歴
  // 部品在庫管理
  // 予防保全計画
}
```

---

## 🏥 医療・福祉系（10個）

### 21. 予約管理（クリニック）
```javascript
// 価格: 7万円 / 開発時間: 45分
function appointmentSystem() {
  // オンライン予約
  // リマインダー送信
  // キャンセル待ち管理
  // 予約統計
}
```

### 22. 患者カルテ管理
```javascript
// 価格: 10万円 / 開発時間: 60分
function patientRecords() {
  // 基本情報管理
  // 診察履歴
  // 処方履歴
  // アレルギー情報
}
```

### 23. 薬品在庫管理
```javascript
// 価格: 8万円 / 開発時間: 50分
function medicineInventory() {
  // 在庫管理
  // 使用期限アラート
  // 自動発注
  // 使用量分析
}
```

### 24. 介護記録
```javascript
// 価格: 7万円 / 開発時間: 45分
function careRecords() {
  // 日々の記録
  // バイタルチェック
  // 服薬管理
  // 家族連絡
}
```

### 25. 訪問スケジュール
```javascript
// 価格: 6万円 / 開発時間: 40分
function visitSchedule() {
  // スケジュール作成
  // ルート最適化
  // スタッフ配置
  // 実績管理
}
```

---

## 🏫 教育系（10個）

### 26. 成績管理
```javascript
// 価格: 6万円 / 開発時間: 40分
function gradeManagement() {
  // 成績入力
  // 自動集計
  // 成績表生成
  // 保護者通知
}
```

### 27. 出席管理
```javascript
// 価格: 5万円 / 開発時間: 30分
function attendanceTracking() {
  // 出席記録
  // 出席率計算
  // 欠席連絡
  // 月次レポート
}
```

### 28. 授業スケジュール
```javascript
// 価格: 6万円 / 開発時間: 40分
function classSchedule() {
  // 時間割作成
  // 教室割り当て
  // 講師スケジュール
  // 変更通知
}
```

### 29. 宿題管理
```javascript
// 価格: 5万円 / 開発時間: 30分
function homeworkManagement() {
  // 課題配布
  // 提出確認
  // 採点記録
  // フィードバック
}
```

### 30. 保護者連絡
```javascript
// 価格: 5万円 / 開発時間: 30分
function parentCommunication() {
  // お知らせ配信
  // 個別連絡
  // アンケート
  // 面談予約
}
```

---

## 🍴 飲食業系（5個）

### 31. 予約台帳
```javascript
// 価格: 6万円 / 開発時間: 40分
function restaurantReservation() {
  // 予約受付
  // 座席管理
  // キャンセル処理
  // 顧客履歴
}
```

### 32. 仕入管理
```javascript
// 価格: 7万円 / 開発時間: 45分
function purchaseManagement() {
  // 発注管理
  // 原価計算
  // 仕入先管理
  // 支払管理
}
```

### 33. メニュー分析
```javascript
// 価格: 5万円 / 開発時間: 30分
function menuAnalysis() {
  // 売上分析
  // ABC分析
  // 原価率管理
  // メニュー改善提案
}
```

### 34. スタッフシフト
```javascript
// 価格: 6万円 / 開発時間: 40分
function staffScheduling() {
  // シフト作成
  // 人件費計算
  // 繁忙期対応
  // シフト交代
}
```

### 35. 日次締め処理
```javascript
// 価格: 5万円 / 開発時間: 30分
function dailyClosing() {
  // 売上集計
  // 現金照合
  // 日報作成
  // 本部報告
}
```

---

## 💡 その他便利ツール（10個）

### 36. タスク管理
```javascript
// 価格: 4万円 / 開発時間: 25分
function taskManagement() {
  // タスク登録
  // 進捗管理
  // 期限アラート
  // ガントチャート
}
```

### 37. 議事録管理
```javascript
// 価格: 4万円 / 開発時間: 25分
function meetingMinutes() {
  // テンプレート生成
  // アクション管理
  // 配布リスト
  // フォローアップ
}
```

### 38. FAQ管理
```javascript
// 価格: 5万円 / 開発時間: 30分
function faqManagement() {
  // Q&A登録
  // カテゴリ分類
  // 検索機能
  // 利用統計
}
```

### 39. 備品管理
```javascript
// 価格: 5万円 / 開発時間: 30分
function assetManagement() {
  // 備品台帳
  // 貸出管理
  // 棚卸し
  // 購入申請
}
```

### 40. イベント管理
```javascript
// 価格: 6万円 / 開発時間: 40分
function eventManagement() {
  // 参加者管理
  // 受付システム
  // アンケート
  // 事後レポート
}
```

---

## 🎯 導入の流れ

### Step 1: テンプレート選択（5分）
1. カタログから選ぶ
2. デモを確認
3. カスタマイズ要望を伝える

### Step 2: カスタマイズ（30分）
1. 企業名・ロゴ設定
2. 項目調整
3. ワークフロー設定

### Step 3: 納品（5分）
1. スプレッドシートコピー
2. 権限設定
3. 使い方説明

### Step 4: サポート（オプション）
1. 操作説明会（1時間）
2. マニュアル作成
3. 保守サポート

---

## 💰 料金プラン

### ベーシック
- テンプレートそのまま: **3万円**
- 納期: 即日
- サポート: メール1ヶ月

### スタンダード
- 軽微なカスタマイズ込み: **5万円**
- 納期: 2営業日
- サポート: メール3ヶ月

### プレミアム
- フルカスタマイズ: **10万円**
- 納期: 5営業日
- サポート: 電話・メール6ヶ月
- 操作説明会付き

### エンタープライズ
- 完全オーダーメイド: **20万円〜**
- 納期: 要相談
- サポート: 1年間
- 定期メンテナンス込み

---

## 📞 お問い合わせ

**「カタログ番号○番のテンプレートが欲しい」**
と伝えるだけでOK！

30分で簡単カスタマイズ、
即日納品可能です。

---

**💡 全テンプレートはAIで効率的に生成・カスタマイズ可能**
**🚀 組み合わせによる統合システムの構築も対応**